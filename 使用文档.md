# QQ 视频解析机器人使用文档

## 1. 项目简介

本项目是一个运行在 Windows 上的 QQ 群视频解析机器人。群成员发送 B站或抖音分享链接后，机器人会解析链接、下载媒体文件，并把视频或图文内容发送回 QQ 群。

当前主要支持：

- B站普通视频链接，例如 `https://www.bilibili.com/video/BV...`
- B站短链接，例如 `https://b23.tv/...`
- 抖音视频分享链接，例如 `https://v.douyin.com/...`
- 抖音图文分享链接

当前限制：

- 视频最长 480 秒（8 分钟）
- 单个资源最大 90 MB
- 只启用 B站和抖音解析
- 机器人服务和 NapCat 仅监听本机，不应直接暴露到公网

## 2. 目录结构

```text
D:\123\ai\QQbot\BOT2
├─ NapCat.Shell              QQ 与 OneBot V11 接入程序
├─ nonebot-plugin-parser     视频解析插件源码及缓存目录
│  └─ cache                  已下载的图片、视频和临时文件
├─ qq-video-bot              NoneBot2 机器人主程序
│  ├─ .venv                  Python 虚拟环境
│  ├─ .env                   运行环境选择
│  ├─ .env.prod              正式运行配置
│  ├─ bot.py                 程序入口
│  ├─ pyproject.toml         依赖和插件配置
│  └─ uv.lock                锁定的依赖版本
└─ 使用文档.md               本文档
```

三个主要目录的职责：

1. `NapCat.Shell` 登录并控制 QQ，把 QQ 消息转换为 OneBot V11 事件。
2. `qq-video-bot` 接收消息事件，加载解析插件并把结果发送回 QQ。
3. `nonebot-plugin-parser` 负责解析 B站、抖音链接，下载并整理媒体文件。

## 3. 环境要求

建议使用以下环境：

- Windows 10、Windows 11 或带桌面的 Windows Server
- Python 3.10 至 3.14，当前环境使用 Python 3.13
- `uv` Python 包管理器
- FFmpeg
- Windows QQ
- 独立的机器人 QQ 账号

检查命令：

```powershell
uv --version
ffmpeg -version
```

如果 PowerShell 提示找不到 `uv`，先关闭并重新打开终端，再执行：

```powershell
where.exe uv
```

FFmpeg 用于合并 B站的音频流和视频流。未安装时可以执行：

```powershell
winget install --id Gyan.FFmpeg
```

安装后重新打开 PowerShell。

## 4. 首次安装

进入机器人目录：

```powershell
cd D:\123\ai\QQbot\BOT2\qq-video-bot
```

按锁定版本安装依赖：

```powershell
uv sync --locked
```

机器人通过 `pyproject.toml` 的 `[tool.uv.sources]` 使用相邻目录中的本地解析插件。该版本包含下载中断后的 `.part` 断点续传和完整长度校验，因此迁移项目时必须同时保留 `qq-video-bot` 与 `nonebot-plugin-parser` 两个目录。

安装完成后，虚拟环境位于：

```text
D:\123\ai\QQbot\BOT2\qq-video-bot\.venv
```

一般不需要手动激活虚拟环境，使用 `uv run` 即可。

## 5. 配置 NapCat

### 5.1 启动 NapCat

右键以管理员身份运行：

```text
D:\123\ai\QQbot\BOT2\NapCat.Shell\launcher.bat
```

NapCat 会启动或接管 Windows QQ。使用专门的机器人 QQ 账号登录，不建议使用重要的个人主账号。

### 5.2 打开管理页面

默认管理地址：

```text
http://127.0.0.1:6099/webui
```

访问令牌保存在：

```text
D:\123\ai\QQbot\BOT2\NapCat.Shell\config\webui.json
```

令牌属于敏感信息，不要发到群聊、截图公开或写入公开仓库。

### 5.3 配置反向 WebSocket

在 NapCat WebUI 的“网络配置”中新建 `WebSocket Client`：

| 配置项     | 值                                    |
| ---------- | ------------------------------------- |
| 启用       | 开启                                  |
| 名称       | `qq-video-bot`，也可以自定义        |
| URL        | `ws://127.0.0.1:8080/onebot/v11/ws` |
| 消息格式   | `Array`                             |
| Token      | 当前本机配置留空                      |
| 心跳间隔   | `30000`                             |
| 重连间隔   | `30000` 或 `5000`                 |
| 开启 Debug | 关闭                                  |

“上报自身消息”只影响机器人账号自己发送的消息是否上报。推荐使用另一个 QQ 账号向群里发送测试链接，不要依赖机器人账号自己触发解析。

如果以后设置 OneBot Token，NapCat 与 `.env.prod` 中的 Token 必须完全一致。

## 6. 启动机器人

先保持 NapCat 和 QQ 正常运行，然后新开一个 PowerShell：

```powershell
cd D:\123\ai\QQbot\BOT2\qq-video-bot
uv run python bot.py
```

正常启动时会看到类似输出：

```text
NoneBot is initializing...
Succeeded to load plugin "nonebot_plugin_parser"
Loaded adapters: OneBot V11
Uvicorn running on http://127.0.0.1:8080
Bot 机器人QQ号 connected
```

出现 `Bot ... connected` 才表示 NapCat 与 NoneBot 已经连接成功。

推荐启动顺序：

1. 启动 QQ 和 NapCat。
2. 启动 `qq-video-bot`。
3. 等待终端出现 `Bot ... connected`。
4. 在测试群发送链接。

两个程序的启动顺序颠倒通常也能自动重连，但首次排查问题时建议使用上述顺序。

## 7. 群内使用

直接在机器人所在群发送完整分享文本或链接，例如：

```text
https://b23.tv/xxxxxxxx
```

```text
复制打开抖音，看看这个作品 https://v.douyin.com/xxxxxxxx/
```

机器人收到链接后通常会：

1. 对原消息添加一个处理中的表情回应。
2. 解析分享链接。
3. 下载封面和视频。
4. 发送作品信息图片。
5. 把视频发送到当前群聊。

首次测试建议选择时间较短、文件较小的公开视频。

### 7.1 只允许指定群解析

当前 `.env.prod` 使用黑名单模式：

```text
PARSER_GROUP_BLACKLIST_ENABLED=true
```

该模式默认允许所有群解析。如果只希望在某个指定群中使用，先修改为：

```text
PARSER_GROUP_BLACKLIST_ENABLED=false
```

重启机器人后，由群主、管理员或 NoneBot 超级用户在目标群发送：

```text
@机器人 开启解析
```

白名单模式下，其他没有执行“开启解析”的群不会处理视频链接。

需要关闭某个群时发送：

```text
@机器人 关闭解析
```

## 8. 当前配置说明

主要配置文件：

```text
D:\123\ai\QQbot\BOT2\qq-video-bot\.env.prod
```

常用配置：

| 配置项                             |        当前值 | 说明                                     |
| ---------------------------------- | ------------: | ---------------------------------------- |
| `HOST`                           | `127.0.0.1` | 只允许本机访问                           |
| `PORT`                           |      `8080` | OneBot 反向 WebSocket 服务端口           |
| `PARSER_DURATION_MAXIMUM`        |       `480` | 最大视频时长，单位为秒                   |
| `PARSER_MAX_SIZE`                |        `90` | 最大下载大小，单位为 MB                  |
| `PARSER_APPEND_URL`              |     `false` | 回复中不附加原链接，避免自身消息重复处理 |
| `PARSER_USE_BASE64`              |     `false` | 使用本地文件路径发送媒体，降低内存占用   |
| `PARSER_GROUP_BLACKLIST_ENABLED` |      `true` | 默认允许所有群解析                       |

当前缓存配置：

```text
LOCALSTORE_PLUGIN_CACHE_DIR={"nonebot_plugin_parser":"D:/123/ai/QQbot/BOT2/nonebot-plugin-parser/cache"}
```

因此视频不会再默认写入 C 盘的 NoneBot 缓存目录。

### 8.1 B站 Cookie

不使用 Cookie 时，必须删除或注释 `PARSER_BILI_CK`，不要留下下面这种空配置：

```text
PARSER_BILI_CK=
```

空配置会被旧版解析器当成 Cookie 字符串，可能报错：

```text
ValueError: not enough values to unpack
```

确实需要 Cookie 时使用完整的 `name=value` 格式，例如：

```text
PARSER_BILI_CK="SESSDATA=实际值;bili_jct=实际值;buvid3=实际值"
```

建议使用专门的 B站测试账号，不要公开 Cookie。

## 9. 缓存管理

解析缓存位于：

```text
D:\123\ai\QQbot\BOT2\nonebot-plugin-parser\cache
```

缓存可能包含：

- `.mp4` 视频
- `.m4s` B站音视频流
- `.jpg`、`.jpeg`、`.webp`、`.png` 封面和图文
- 渲染后的作品信息图片

停止机器人后可以直接删除过期缓存。删除缓存不会破坏程序，但再次解析相同链接时需要重新下载。

建议定期检查目录大小：

```powershell
$cache = 'D:\123\ai\QQbot\BOT2\nonebot-plugin-parser\cache'
$files = Get-ChildItem -LiteralPath $cache -Recurse -File
[math]::Round((($files | Measure-Object Length -Sum).Sum / 1GB), 2)
```

## 10. 停止与重启

停止 NoneBot：在运行 `uv run python bot.py` 的终端按 `Ctrl+C`。

重新启动：

```powershell
cd D:\123\ai\QQbot\BOT2\qq-video-bot
uv run python bot.py
```

如果修改了 `.env.prod`、NapCat 网络配置或机器人 QQ 账号，建议完整重启 NapCat 和 NoneBot。

## 11. 常驻运行

本机电脑关机后，机器人无法继续运行。需要把整个项目迁移到一台持续开机的 Windows 电脑或 Windows 云服务器。

本机常驻时可使用 Windows“任务计划程序”：

- 登录 Windows 后启动 `NapCat.Shell\launcher.bat`
- 延迟约 30 秒启动 `.venv\Scripts\python.exe bot.py`
- 设置任务失败后自动重启
- 禁用系统自动睡眠
- BIOS 开启断电恢复后自动开机

云服务器也应使用 Windows，并至少配置 2 核 CPU、4 GB 内存、80 GB SSD 和 8 Mbps 带宽。QQ、NapCat 和 NoneBot 应安装在同一台云服务器上，`6099` 与 `8080` 仍然只监听本机。

## 12. 常见问题

### 12.1 浏览器访问 8080 显示 Not Found

访问下面地址时显示 `{"detail":"Not Found"}` 是正常现象：

```text
http://127.0.0.1:8080
```

`8080` 是 OneBot WebSocket 服务，不是管理网页。NapCat 管理页面通常是：

```text
http://127.0.0.1:6099/webui
```

### 12.2 群里发送链接没有反应

依次检查：

1. QQ 和 NapCat 是否仍在运行。
2. NoneBot 是否显示 `Bot ... connected`。
3. NapCat WebSocket Client 是否启用。
4. URL 是否为 `ws://127.0.0.1:8080/onebot/v11/ws`。
5. NapCat 与 NoneBot 的 Token 是否一致。
6. 链接是否由另一个 QQ 账号发送，而不是机器人账号自身发送。
7. 当前群是否处于插件白名单或没有进入黑名单。

### 12.3 抖音可以解析，B站失败

首先检查 `.env.prod` 中是否存在空的：

```text
PARSER_BILI_CK=
```

如果没有真实 Cookie，应删除或注释这一行。

如果日志出现 B站错误码 `-509`，表示请求频率过高或触发风控。等待一段时间再试，必要时配置专用账号 Cookie。

### 12.4 视频信息解析成功，但没有发送视频

可能原因：

- 视频超过 480 秒
- 文件超过 90 MB
- QQ 上传超时或群文件限制
- 磁盘空间不足
- FFmpeg 不可用
- B站或抖音临时限制下载请求

检查 NoneBot 终端中最后一段错误信息，并先换一个小视频测试。

### 12.5 端口 8080 被占用

检查占用进程：

```powershell
netstat -ano | findstr :8080
```

不要同时启动两个 `bot.py`。如果确实需要修改端口，必须同时修改 `.env.prod` 的 `PORT` 和 NapCat WebSocket Client URL。

### 12.6 uv 命令无法识别

关闭并重新打开 PowerShell，然后检查：

```powershell
where.exe uv
uv --version
```

也可以绕过 `uv`，直接使用已经创建的虚拟环境：

```powershell
cd D:\123\ai\QQbot\BOT2\qq-video-bot
.\.venv\Scripts\python.exe bot.py
```

## 13. 安全与使用边界

- NapCat 使用非官方 QQ 接入方式，存在账号风控风险。
- 使用独立机器人账号，不要使用重要的个人主账号。
- 不要把 WebUI Token、QQ 登录信息、B站 Cookie 提交到公开仓库。
- 不要向公网开放 NapCat WebUI、OneBot WebSocket 或远程桌面。
- 只解析有权使用的内容，不要大规模抓取、传播或长期保存受版权保护的视频。
- 建议先在小型私人测试群验证，再逐步扩大使用范围。

## 14. 快速启动清单

每次启动时按下面顺序检查：

1. Windows QQ 已登录机器人账号。
2. NapCat 已启动。
3. NapCat WebSocket Client 已启用。
4. 在 `qq-video-bot` 目录执行 `uv run python bot.py`。
5. 终端出现 `Bot ... connected`。
6. 在测试群发送一个小型 B站或抖音链接。
7. 确认信息卡片和视频都成功发送。

## 15. 开源项目与许可证

本项目使用了以下开源项目：

- [nonebot-plugin-parser](https://github.com/fllesser/nonebot-plugin-parser)：链接解析插件，原作者为 Les Freire / fllesser，采用 MIT License。本项目使用了针对当前场景的本地修改版，原许可证保留在 `nonebot-plugin-parser/LICENSE`。
- [NoneBot2](https://github.com/nonebot/nonebot2)：机器人应用框架。
- [NapCatQQ](https://github.com/NapNeko/NapCatQQ)：用于连接 Windows QQ 和 OneBot V11。NapCat 运行时、账号配置和登录状态不包含在本仓库中，使用者需要自行下载和安装。

本项目的修改和文档不代表上述开源项目作者对本项目提供支持、维护或背书。第三方组件分别适用其各自的许可证。
